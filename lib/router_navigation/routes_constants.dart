///here we will define all the route constants so that it remains uniform throughout the application
enum AdsType { dialog, bigBanner, smallBanner, singleBanner }

const String mainRoute = 'AdsScreen';
